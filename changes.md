the Purchase Order flow with stages Draft → Submitted → Approved → Received, shown as a colored status badge in each table row. The actions button per row changes by status and role: a Draft shows Edit, Submit, Delete; a Submitted PO shows Approve and Reject to the Admin but only Recall to Draft to the Manager who created it; an Approved PO shows Create GRN and Download PDF. The creator prepares an editable Draft, then Submit locks it and notifies the approver. If an Admin created it, it auto-approves; if a Manager created it, it routes to Admin (always, or only above an amount set in Approval Rules). Reject requires a reason and sends it back as editable. On approval the badge turns green and the creator is notified. Only Approved POs appear in the GRN dropdown. Make the table filterable by status, and build this as a reusable approval pattern for GRN, QC, adjustments, and transfers

Colored status badge, actions should be visible propery 
Same pattern follow for  GRN QC, adjustments , transfer and for all.